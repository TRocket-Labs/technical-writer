> **Template:** Quickstart Page  
> **Based on:** [Resend's Node.js Quickstart](https://resend.com/docs/send-with-nodejs)  
> **Why it works:** Resend prioritizes the "Time to First Hello World" by stripping away all non-essential configuration. The documentation mirrors the product philosophy: a single install command, a single code block, and a direct path to a successful API call.

---

# **{Product Name} Quickstart — {Language/Framework}**

💡 _One sentence summary of what you will achieve. Keep it focused on the end result._

**Example:** "Learn how to send your first email using {Framework} and the {Product} SDK in under 5 minutes."

---

## **Prerequisites**

💡 _List only the absolute minimum items required. High friction here kills conversion._

- [ ] {Requirement 1: e.g., Resend API Key}
- [ ] {Requirement 2: e.g., Verified Domain}
- [ ] {Requirement 3: e.g., Node.js installed}

---

## **1. Install**

💡 _Provide the single installation command. If multiple package managers are used, use a tabbed interface or list the most common one._

```bash
{package-manager} install {your-sdk}
```

---

## **2. {Core Action}**

💡 _Provide a complete, runnable code block. Use realistic variable names but keep the logic dead simple._

**Example:** "Send an email using HTML"

```{language}
{Full working code example with SDK initialization and main method}
```

---

## **3. Try it yourself**

💡 _Give users a way to see the full context without cluttering this guide. Linking to a GitHub "ready-to-run" example is best._

[{Framework} Example — See the full source code on GitHub.]({link to GitHub repo})

---

ℹ️ _**Pattern Note:** This template follows that "Code-First" approach: Prerequisites → Install → Execute. Anything that isn't required for the first successful API call should be moved to a "Next Steps" or "Guides" section._
